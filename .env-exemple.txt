# Pour ingest.py et fastapi
DATABASE_URL=postgresql://{{postgres_username}}:{{postgres_password}}@prediclection-db-container:5432/predilection

# Identifiants Communs
POSTGRES_DB=predilection
POSTGRES_USER=usernamepostgres
POSTGRES_PASSWORD=postgres_password
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Pour Django
SECRET_KEY='secret key pour django'
DEBUG=False
BASE_URL_LOCAL=http://fastapi-container:8000
BASE_URL=https://geo.api.gouv.fr

# Production — Traefik & Domaine
# DOMAIN_NAME : le nom de domaine pointant vers le VPS (ex: predilection.example.com)
DOMAIN_NAME=your-domain.example.com

# TRAEFIK_DASHBOARD_CREDENTIALS : généré avec htpasswd -nb admin <mot_de_passe> | sed -e s/\\$/\\$\\$/g
TRAEFIK_DASHBOARD_CREDENTIALS=admin:$$apr1$$xxxxxxx$$xxxxxxxxxxxxxxxxxxxxxxx

# Production — CI/CD (secrets GitHub Actions à configurer dans Settings > Secrets)
# VPS_HOST      : adresse IP ou hostname du VPS (ex: 164.132.43.252)
# VPS_USER      : utilisateur SSH de déploiement (ex: amaury)
# VPS_SSH_KEY   : clé privée SSH dédiée au déploiement (générée séparément)
